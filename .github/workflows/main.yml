name: x0D0D0P
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a Python script
        run: |
          ip address
          echo test, ---------------------------------------.
                  
       
