name: x0D0D0P

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#---------------------------------------------------preparation
    - name: update
      run: sudo apt update
#---------------------------------------------------N0G0R0O0K
    - name: N0J0R0Ok install 
      run: sudo snap install ngrok      
    - name: N0J0R0Ok AUTH 
      run: sudo ngrok authtoken tKuOSxi2qkXJh2O3VrKmYlSi4t_2BQ9W6dyHsaefEkLhuGbk
    - name: N0J0R0Ok R0D0P
      run: sudo ngrok tcp 3389
#---------------------------------------------------x0R0R0R
    - name: install x0R0R0R
      run: sudo apt install xrdp
    - name: activate x0R0R0R
      run: sudo systemctl enable xrdp
    - name: open P0OR0T
      run: sudo ufw allow from any to any port 3389 proto tcp
    - name: show I0P
      run: ip address

      
#  - name: finnal message
#     run:  echo test, ---------------------------------------.
      
 #  - name: update
  #   run: sudo apt update
   #- name: update
    # run: sudo apt update
    
    
    #  - uses: actions/checkout@v3
    #  - name: the x0R0D0P
    #    run: |
    #      ip address
    #      echo test, ---------------------------------------.
                  
       
